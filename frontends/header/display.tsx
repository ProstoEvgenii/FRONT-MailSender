import { Cemjsx } from "cemjs-all"
import Navigation from "./navigation"

export const display = function () {
    return (
        <header class="header">
            <div class="wrapper">
                <Navigation />
            </div>
        </header>
    )
}